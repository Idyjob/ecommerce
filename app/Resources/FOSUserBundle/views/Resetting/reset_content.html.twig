{% trans_default_domain 'FOSUserBundle' %}


<div class="row">
        <div class="col-md-12 col-lg-12 col-xs-12 col-sm-12">
            <h2>Mot de passe perdu</h2>
            <div id="collapseOne" class="accordion-body collapse in">
                <div class="accordion-inner">
                    <div class="col-md-4 col-lg-4 col-xs-4 col-sm-4">
                        <h4>Récupération du mot de passe ?</h4>
                        <em>
                            Compléter le formulaire, votre mot de passe sera alors réinitialiser.<br /><br />
                            Une fois validé, vous devrez utiliser le nouveau mot de passe que vous avez défini.
                        </em>
                    </div>

                    <div class="col-md-4 col-md-offset-2">
                        <form action="{{path('fos_user_resetting_reset', {'token': token})}}" token="{{token}}" class="fos_user_resetting_reset">
                            {{ form_widget(form) }}
                            <br/>
                            <input type="submit" value="{{ 'resetting.reset.submit'|trans }}"  class="btn btn-primary"/>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
